# {{BOOK_TITLE}} — Reconstruction Workspace

This workspace reconstructs a provisional model of the book from source-linked
evidence. It is not a claim that the author’s private intention is known.

Start with [guide/dashboard.md](guide/dashboard.md), then inspect `model/` and
follow evidence links into `units/`, `notes/`, and `evidence/`.

## Working status

See `workspace.json` for the deliberately small status vocabulary. A first run
is usable when it makes its evidence, confidence, alternatives, and gaps clear.
